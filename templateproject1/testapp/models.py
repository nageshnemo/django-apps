from django.db import models

# Create your models here.
# all the database thing is done in models.def foo():
    doc = "The  property."
    def fget(self):
        return self._
    def fset(self, value):
        self._ = value
    def fdel(self):
        del self._
    return locals()
 = property(**())
